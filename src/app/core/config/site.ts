/** Canonical origin used for absolute URLs in OG tags, JSON-LD and the
 *  sitemap. Points at the GCP deployment behind Caddy (also referenced by
 *  scripts/generate-sitemap.ts). */
export const SITE_ORIGIN = 'https://bookstore.quocnamtran.com';
export const SITE_NAME = 'Bookstore';
